import { useRef } from "react";

import { type VariantProps, cva } from "class-variance-authority";
import { type AriaCheckboxProps, useCheckbox } from "react-aria";

const Checkbox: React.FC = () => null;

export default Checkbox;
