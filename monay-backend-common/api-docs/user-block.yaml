paths:
  /user/block:
    post:
      tags:
        - User block
      summary: Block or Unblock User
      security:
        - BearerAuth: []
      produces:
        - application/json
      parameters:
        - in: body
          name: body
          required: true
          schema:
            "$ref": "#/definitions/user-block"    
      responses:
        '200':
          description: OK
    get:
      tags:
        - User block
      summary: Block User list
      security:
        - BearerAuth: []
      produces:
        - application/json
      parameters:
        - in: query
          name: limit
          type: integer
          required: false
        - in: query
          name: offset
          type: integer
          required: false    
      responses:
        '200':
          description: OK                     
definitions:
  user-block:
    type: object
    properties:
      blockUserId:
        type: string
        required: true     